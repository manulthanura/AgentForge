[project]
name = "agentforge"
version = "0.1.0"
description = "Autonomous AI agent with tool use and human-in-the-loop approval gates"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.116.0",
    "fastapi>=0.139.0",
    "httpx>=0.28.1",
    "langgraph>=1.2.7",
    "langgraph-checkpoint-postgres>=3.1.0",
    "openai>=2.44.0",
    "psycopg[binary]>=3.3.4",
    "pygithub>=2.9.1",
    "python-dotenv>=1.2.2",
    "tree-sitter>=0.26.0",
    "tree-sitter-python>=0.25.0",
    "uvicorn>=0.49.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]
