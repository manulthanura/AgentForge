"""Workflow orchestration — the aggregate-root context.

Owns the agent workflow lifecycle and coordinates the other bounded
contexts' use cases (issue_intake, code_intelligence, fix_generation, ...).
"""
